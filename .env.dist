# Project Configuration
PROJECT_NAME=storebunk-pos

# Docker User Configuration (for proper file permissions)
# Set these to match your host user ID and group ID
# Run `id -u` and `id -g` to get your values
UID=1000
GID=1000
USER=appuser

# GitHub Personal Access Token
# Required for accessing private repositories during Docker build
# Create a token at: https://github.com/settings/tokens
# Required scopes: repo (Full control of private repositories)
GITHUB_TOKEN=your_github_personal_access_token_here
